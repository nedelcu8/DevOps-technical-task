## Simple go cli app

- docker file that is used to build go app in staged and then use the go app as an entrypoint 
- build stage create the image of the go app
- test container execute the app with some input 